{
  "name": "@ms-shared-ticketing/common",
  "version": "1.0.6",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "del-cli ./build/*",
    "pub": "git add . && git commit -m \"New Updates to package \" &&npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^4.9.4"
  }
}
